skew is a transform function that takes 2 arguments, one to shear x axis by and one to shear y axis by

positioning a psuedo ::before element closest relative to its closest positioned ancestor is done by absolute, not relative

opacity is a CSS value shortcut where you dont have to use rgba

target .face element with .right class without a space in selector
.face.right{
}

you can invert x-axis using the transform function scaleX(-1)
further changes to transform (like in an animation) you must continue to 
define the scaleX change

scale defines both dimensions

cursor: not-allowed to show cant move penguin

can apply same properties from animations to transitions
