the value of an input element is always string

filter() method allows you to filter elements out of an array and create a new one in the process

many array methods can be often chained together

reduce() method takes an array and applies a callback function to condense into a single value

normally form submission triggers a page referesh, add return false; in onsubmit= property of form to stop that

calculate using a mode and the numbers as keys and incrementing the value

edge cases for mode include when every value occurs same amount

use a set for this case. Set is a datastructure that only allows unique values.
passing an array into a set constructor removes duplicates

.sort() mutates an array it is called on, and it's made practice to mutate a function parameter.

call .slice() to make a shallow copy before making changes
